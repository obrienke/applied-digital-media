let length = +prompt("Enter length of rectangle");
let width = +prompt("Enter width of rectangle");
let unit = prompt("Enter unit of measurement");

alert("Area of rectangle is: " + (length * width) + " " + unit + " squared.");

